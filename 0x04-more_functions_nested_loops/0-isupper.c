#include <stdio.h>

/**
 *main - check if letter is uppercase
 *
 *@c : parameter int c
 *Return : 0 if not 1 if upper
 *
 */
int main( int c)
{
  if (isupper (c))
    {
      return (0);
    }
  else
    {
      return (0);
    }
}
